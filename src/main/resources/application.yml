#These settings are used only for local dev
# Use with the DB set up in localdev.yml (See README.md)
logging:
  level:
    # Hides HHH000424: Disabling contextual LOB creation as createClob() method threw error
    org.hibernate.engine.jdbc.env.internal.LobCreatorBuilderImpl: WARN

spring:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: localdev
    driver-class-name: org.postgresql.Driver

  jpa:
    database: POSTGRESQL
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
      properties:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  liquibase:
    change-log: classpath:/db/changelog/master.yml

  security:
    oauth2:
      client:
        registration:
          okta:
            client-id: 0oaj46fad5TzDaHvE0h7
            client-secret: SQU1-DBPsdQEOu_k8t_dhEPzXm0G2VPkYUrmEI2Q
            scope: openid, email, profile
        provider:
          okta:
            issuer-uri: https://dev-879532.oktapreview.com/oauth2/default